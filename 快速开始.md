# 快速开始指南

## 🎯 最简单的使用方法

### 第一步：准备你的 PSD 文件

在 Photoshop 中打开你绘制好的角色皮肤文件。

### 第二步：运行脚本

1. 在 Photoshop 中：`文件` → `脚本` → `浏览...`
2. 选择这个文件：`D:\Tools\ps-spine-automation\spine_organizer.jsx`
3. 会弹出一个对话框

### 第三步：使用工具

对话框中有几个按钮：

#### 🔍 分析图层
- 点击后显示当前文档有多少个图层
- 按类别分组统计
- 不会修改任何内容，只是预览

#### 🚀 自动整理
- **输入风格名称**（重要！）例如：`racing-onesie`、`cat-onesie`
- 点击后会：
  - 创建分类文件夹（foot、bottom、tops、hair、acc 等）
  - 重命名所有图层为 Spine 规范格式
  - 将图层移动到对应文件夹
- ⚠️ 这会修改你的文档，建议先保存备份

#### 📄 生成报告
- 生成一个文本文件，列出所有命名建议
- 不会修改文档
- 可以先看看脚本会怎么处理

## 📝 风格名称示例

根据你的 JSON 文件，风格名称应该是：

- `racing-onesie`（赛车连体服）
- `alien-onesie`（外星人连体服）
- `angel`（天使）
- `cat-onesie`（猫咪连体服）
- `cockroach`（蟑螂）
- `deer`（鹿）
- `heartbreaker`（心碎者）
- `pinkboy`（粉色男孩）

## ⚡ 工作流程示例

```
你的原始图层：
├─ 图层1
├─ 鞋子左
├─ 鞋子右
├─ 衣服
└─ 头发

↓ 运行脚本（输入风格名: my-style）

整理后：
📁 foot
│  ├─ shoes-l-my-style
│  └─ shoes-r-my-style
📁 tops
│  └─ sleeve-body-my-style
📁 hair
   └─ hair-front-my-style
```

## 🔧 图层命名提示

为了让脚本更准确地识别图层，命名时包含这些关键词：

| 部位 | 关键词 |
|------|--------|
| 鞋子 | shoe, shoes |
| 袜子 | sock, socks |
| 裤子 | pant, pants |
| 裙子 | skirt |
| 上衣 | top, sleeve, shirt |
| 头发 | hair |
| 眼睛 | eye |
| 嘴巴 | mouth |
| 眉毛 | eyebrow |
| 配饰 | acc, wing, tail, glasses, headband |

**左右标记**：
- 左：`-l`、`-left`、`左`
- 右：`-r`、`-right`、`右`

**前后标记**：
- 前：`-front`、`前`
- 后：`-back`、`后`

## 🆘 常见问题

### 问：脚本处理后名称不对怎么办？
答：运行后可以手动调整。或者先点"生成报告"查看，如果不满意就修改图层名后重新运行。

### 问：我想撤销怎么办？
答：Photoshop 的撤销功能可用（Ctrl+Z），或者在运行前先保存一个版本。

### 问：脚本能处理图层组吗？
答：目前只处理普通图层。如果你的图层在组里，需要先移出来。

### 问：可以处理智能对象吗？
答：建议先栅格化智能对象，再运行脚本。

## 🎬 完整工作流

1. **画图** → 在 PS 中绘制角色各部位
2. **命名** → 图层大致命名（包含关键词即可）
3. **运行脚本** → 自动整理和重命名
4. **检查** → 查看结果，手动微调
5. **导出** → 使用 `pstospine` 脚本导出
6. **导入 Spine** → 在 Spine 中使用

## 💡 提示

- 第一次使用建议先在测试文件上试验
- 风格名称在整个项目中保持一致
- 图层名称包含英文关键词，脚本识别更准确
- 运行前先保存文档

---

**遇到问题？** 检查：
1. ✅ Photoshop 版本是否支持 JSX（CC 2015+）
2. ✅ 文档是否已打开
3. ✅ 图层是否为普通图层（非智能对象）
