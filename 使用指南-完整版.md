# Spine 图层整理工具 - 完整使用指南

## 📦 工具简介

这个工具可以自动将 Photoshop 中的图层整理为 Spine 动画引擎所需的标准结构，只需用中文命名图层，点一下按钮就能自动生成规范的三层嵌套结构。

---

## 🚀 使用步骤

### 第一步：准备 PSD 文件

1. **文件命名规则**：PSD 文件名就是风格名
   - 例如：`school-blazer.psd` → 风格名是 `school-blazer`
   - 例如：`deer.psd` → 风格名是 `deer`
   - 风格名会自动添加到所有图层的末尾

2. **在 Photoshop 中打开这个 PSD 文件**

### 第二步：按规范命名图层

按照下面的**命名规范表**，用中文给每个图层命名。

> ⚠️ **重要**：只命名可见的图层，隐藏的图层会被自动跳过

### 第三步：运行脚本

1. 在 Photoshop 中，选择菜单：**文件 → 脚本 → 浏览**
2. 找到并选择 `spine_organizer.jsx` 文件
3. 会弹出一个对话框，风格名已自动填入（从文件名提取）
4. 点击 **"预览"** 按钮查看整理效果（可选）
5. 确认无误后，点击 **"开始整理"** 按钮

### 第四步：查看结果

脚本会自动完成以下操作：
- ✅ 栅格化智能对象和文字图层
- ✅ 按三层结构重新组织图层（slot → skin → attachment）
- ✅ 重命名所有图层为规范格式
- ✅ 验证命名正确性并显示报告

---

## 📋 图层命名规范（完整版）

### 一、基础部位（必须完全按照这个命名）

#### 1. 头发

| 中文命名 | 说明 |
|---------|------|
| `头发-前发` 或 `前发` 或 `刘海` | 前面的头发 |
| `头发-后发` 或 `后发` | 后面的头发 |

#### 2. 鞋袜

| 中文命名 | 说明 |
|---------|------|
| `鞋袜-左` 或 `左鞋` | 左脚的鞋 |
| `鞋袜-右` 或 `右鞋` | 右脚的鞋 |
| `左袜` | 左脚的袜子（短袜）|
| `右袜` | 右脚的袜子（短袜）|
| `鞋袜-左鞋角度1` 或 `左鞋角度2` | 左鞋的角度变体（多角度版本）|
| `鞋袜-右鞋角度1` 或 `右鞋角度2` | 右鞋的角度变体（多角度版本）|

> 💡 **角度变体**：图层名包含「角度」关键字时，自动识别为多角度版本。左鞋角度使用 `slot-shoes-l` 结构，右鞋角度使用 `slot-shoes-r` 结构，图层名为 `shoes-{l/r}-new-angle-{风格}`。每个角度创建独立的同名 slot 组。

#### 3. 上装（袖子和身体）

**如果有上下袖子分离的情况：**

| 中文命名 | 说明 |
|---------|------|
| `上装-左上袖子` 或 `左上袖` | 左手上臂部分 |
| `上装-右上袖子` 或 `右上袖` | 右手上臂部分 |
| `上装-左下袖子` 或 `左下袖` | 左手下臂部分 |
| `上装-右下袖子` 或 `右下袖` | 右手下臂部分 |
| `上装-身体` 或 `身体` 或 `上衣` | 上衣主体 |

**如果袖子是一体的（没有上下之分）：**

| 中文命名 | 说明 |
|---------|------|
| `上装-左袖子` 或 `左袖` | 左手袖子 |
| `上装-右袖子` 或 `右袖` | 右手袖子 |
| `上装-身体` 或 `身体` 或 `上衣` | 上衣主体 |

> 💡 **脚本会智能检测**：如果只有上袖子没有下袖子，会自动简化命名

#### 4. 下装（裤子和裙子）

**裤子（分长短）：**

| 中文命名 | 说明 |
|---------|------|
| `下装-左长裤` 或 `左长裤` | 左腿长裤 |
| `下装-右长裤` 或 `右长裤` | 右腿长裤 |
| `下装-左短裤` 或 `左短裤` 或 `左裤` | 左腿短裤 |
| `下装-右短裤` 或 `右短裤` 或 `右裤` | 右腿短裤 |

**裙子（分长短）：**

| 中文命名 | 说明 |
|---------|------|
| `裙子` 或 `短裙` | 短裙 |
| `长裙` | 长裙 |

#### 5. 五官（眉毛和嘴巴）

| 中文命名 | 说明 |
|---------|------|
| `头发-左眉毛` 或 `左眉` | 左边眉毛 |
| `头发-右眉毛` 或 `右眉` | 右边眉毛 |
| `嘴` | 嘴巴 |

#### 6. 眼睛（完整结构）

| 中文命名 | 说明 |
|---------|------|
| `眼睛-左眼白` 或 `左眼白` | 左眼白色部分 |
| `眼睛-右眼白` 或 `右眼白` | 右眼白色部分 |
| `眼睛-左眼珠` 或 `左瞳` 或 `左眼珠` 或 `左眼球` | 左眼瞳孔/虹膜 |
| `眼睛-右眼珠` 或 `右瞳` 或 `右眼珠` 或 `右眼球` | 右眼瞳孔/虹膜 |
| `眼睛-左睫毛` 或 `左睫` 或 `左睫毛` | 左眼睫毛 |
| `眼睛-右睫毛` 或 `右睫` 或 `右睫毛` | 右眼睫毛 |

---

### 二、配饰部位（动态识别）

配饰的命名格式是固定的，但具体物品名会被自动识别并转换。

#### 1. 手持物品

**命名格式：** `配饰-左手拿{物品}` 或 `配饰-右手拿{物品}`

**支持的物品清单：**

| 中文物品名 | 英文代码 | 示例命名 |
|-----------|---------|---------|
| 平底锅 | pan | `配饰-左手拿平底锅` |
| 荧光棒 | lightstick | `配饰-右手拿荧光棒` |
| 法杖 | wand | `配饰-左手拿法杖` |
| 包 | bag | `配饰-右手拿包` |
| 伞 | umbrella | `配饰-左手拿伞` |
| 剑 | sword | `配饰-右手拿剑` |
| 书 | book | `配饰-左手拿书` |
| 花 | flower | `配饰-右手拿花` |
| 灯笼 | lantern | `配饰-左手拿灯笼` |
| 手机 | phone | `配饰-右手拿手机` |
| 相机 | camera | `配饰-左手拿相机` |
| 杯子 | cup | `配饰-右手拿杯子` |
| 球 | ball | `配饰-左手拿球` |
| 旗 | flag | `配饰-右手拿旗` |
| 扇子 | fan | `配饰-左手拿扇子` |
| 喇叭 | trumpet | `配饰-右手拿喇叭` |
| 话筒 / 麦克风 | microphone | `配饰-左手拿话筒` |

**生成结果示例：**
- `配饰-左手拿法杖` → 生成图层名：`hold-l-wand-deer`（假设风格名是 deer）
- `配饰-右手拿包` → 生成图层名：`hold-r-bag-deer`

#### 2. 身体前配饰

**命名格式：** `配饰-身体前{物品}`

**支持的物品清单：**

| 中文物品名 | 英文代码 | 示例命名 |
|-----------|---------|---------|
| 项链 | necklace | `配饰-身体前项链` |
| 项圈 / 颈圈 / 脖圈 | choker | `配饰-身体前项圈` |
| 腰带 | belt | `配饰-身体前腰带` |
| 衬衫 | shirt | `配饰-身体前衬衫` |
| 领带 | tie | `配饰-身体前领带` |
| 领结 | bowtie | `配饰-身体前领结` |
| 围巾 | scarf | `配饰-身体前围巾` |
| 胸针 | brooch | `配饰-身体前胸针` |
| 徽章 | badge | `配饰-身体前徽章` |

**生成结果示例：**
- `配饰-身体前项链` → 生成图层名：`necklace-front-deer`
- `配饰-身体前腰带` → 生成图层名：`belt-front-deer`

#### 3. 身体后配饰

**命名格式：** `配饰-身体后{物品}`

**支持的物品清单：**

| 中文物品名 | 英文代码 | 示例命名 |
|-----------|---------|---------|
| 背包 | backpack | `配饰-身体后背包` |
| 尾巴 | tail | `配饰-身体后尾巴` |
| 翅膀 / 翼 | wings | `配饰-身体后翅膀` |
| 披风 | cape | `配饰-身体后披风` |
| 斗篷 | cloak | `配饰-身体后斗篷` |

**生成结果示例：**
- `配饰-身体后尾巴` → 生成图层名：`tail-back-deer`
- `配饰-身体后翅膀` → 生成图层名：`wings-back-deer`

#### 4. 头部配饰

**命名格式：** `配饰-头部{物品}`

**支持的物品清单：**

| 中文物品名 | 英文代码 | 示例命名 |
|-----------|---------|---------|
| 皇冠 | crown | `配饰-头部皇冠` |
| 发箍 / 发带 / 大眼发箍 | headband | `配饰-头部发箍` |
| 鹿角 | antler | `配饰-头部鹿角` |
| 发夹 | hairpin | `配饰-头部发夹` |
| 帽子 / 贝雷帽 | beaniel | `配饰-头部帽子` |
| 眼镜 | glasses | `配饰-头部眼镜` |
| 耳环 / 耳钉 | earring | `配饰-头部耳环` |
| 耳机 | headphone | `配饰-头部耳机` |
| 兔耳 | bunnyear | `配饰-头部兔耳` |
| 猫耳 | catear | `配饰-头部猫耳` |
| 光环 / 天使圈 | halo | `配饰-头部光环` |

**生成结果示例：**
- `配饰-头部皇冠` → 生成图层名：`crown-front-deer`
- `配饰-头部眼镜` → 生成图层名：`glasses-front-deer`

---

## ✨ 自动智能功能

### 1. 跳过隐藏图层
- 所有隐藏的图层（例如参考图、草稿层）会被自动忽略
- 只有可见的图层才会被整理

### 2. 智能袖子检测
- **如果图层中有上下袖子**（左上袖、左下袖等）：会生成完整的 `sleeve-l-upper` 和 `sleeve-l-lower`
- **如果只有上袖子**（左袖、右袖）：会自动简化为 `sleeve-l` 和 `sleeve-r`

### 3. 连体衣服检测
- 如果文件名包含 `onesie`（例如 `deer-onesie.psd`），会自动识别为连体衣服
- 上装和下装会共用同一个 skin 名称

### 4. 角度变体检测
- 图层名包含「角度」关键字时，自动识别为多角度版本
- **左鞋角度**：使用 `slot-shoes-l` 结构，图层名 `shoes-l-new-angle-{风格}`
- **右鞋角度**：使用 `slot-shoes-r` 结构，图层名 `shoes-r-new-angle-{风格}`
- 每个角度创建独立的同名 slot 组（在 PSD 里是分开的，导出到 Spine 后自动合并）
- 适用于同一鞋子的不同角度版本管理

### 5. 自动栅格化
- 智能对象图层、文字图层会被自动转换为普通图层
- 不会影响原始图层的视觉效果

### 6. 完整验证
整理完成后，脚本会自动检查：
- ✅ 同类部位是否共享相同的 skin（例如左右头发共用 `hair/hair-{风格}`）
- ✅ 所有 skin 和 attachment 是否都包含风格名后缀
- ✅ slot、skin、attachment 的命名格式是否正确
- ✅ 是否存在空的 skin 组
- ⚠️ 左右成对部位是否都存在（警告级别）

---

## 📊 生成结构示例

### 示例 1：头发（前后共享 skin）

**原始图层命名：**
```
头发-前发
头发-后发
```

**整理后结构：**（假设文件名是 `school-blazer.psd`）
```
[slot]slot-hair-front
  [skin]hair/hair-school-blazer
    hair-front-school-blazer

[slot]slot-hair-back
  [skin]hair/hair-school-blazer
    hair-back-school-blazer
```

### 示例 2：手持物品（左右独立）

**原始图层命名：**
```
配饰-左手拿法杖
配饰-右手拿包
```

**整理后结构：**（假设文件名是 `deer.psd`）
```
[slot]slot-acc-l-handheld
  [skin]acc/hold-l-wand-deer
    hold-l-wand-deer

[slot]slot-acc-r-handheld
  [skin]acc/hold-r-bag-deer
    hold-r-bag-deer
```

### 示例 3：头部配饰

**原始图层命名：**
```
配饰-头部皇冠
```

**整理后结构：**（假设文件名是 `princess.psd`）
```
[slot]slot-acc-headwear
  [skin]acc/crown-front-princess
    crown-front-princess
```

### 示例 4：完整眼睛

**原始图层命名：**
```
眼睛-左眼白
眼睛-右眼白
眼睛-左眼珠
眼睛-右眼珠
眼睛-左睫毛
眼睛-右睫毛
```

**整理后结构：**（假设文件名是 `deer.psd`）
```
[slot]slot-eye-white-l
  [skin]eye/eye-deer
    eye-white-l-deer

[slot]slot-eye-white-r
  [skin]eye/eye-deer
    eye-white-r-deer

[slot]slot-irides-l
  [skin]eye/eye-deer
    irides-l-deer

[slot]slot-irides-r
  [skin]eye/eye-deer
    irides-r-deer

[slot]slot-eyelash-l
  [skin]eye/eye-deer
    eyelash-l-deer

[slot]slot-eyelash-r
  [skin]eye/eye-deer
    eyelash-r-deer
```

> 注意：所有眼睛相关图层共享同一个 skin：`eye/eye-deer`

### 示例 5：角度变体（独立分组）

**原始图层命名：**
```
鞋袜-左鞋角度1
鞋袜-左鞋角度2
鞋袜-右鞋角度1
```

**整理后结构：**（假设文件名是 `demo.psd`）
```
[slot]slot-shoes-l
  [skin]foot/foot-demo
    shoes-l-new-angle-demo

[slot]slot-shoes-l
  [skin]foot/foot-demo
    shoes-l-new-angle-demo

[slot]slot-shoes-r
  [skin]foot/foot-demo
    shoes-r-new-angle-demo
```

> 注意：
> - 每个角度变体创建**独立的同名 slot 组**（在 PSD 里是物理分开的）
> - 左鞋角度使用 `slot-shoes-l` 结构，右鞋角度使用 `slot-shoes-r` 结构
> - 图层名带 `-new-angle` 标识，方便识别
> - 导出到 Spine 后，同名 slot 会自动合并成一个 slot

---

## ⚠️ 常见注意事项

### 1. 文件命名
- ✅ PSD 文件名就是风格名，会自动提取
- ✅ 文件名推荐用英文小写和连字符，例如：`school-blazer.psd`、`deer.psd`
- ❌ 不要用中文作为文件名（可能导致风格名识别问题）

### 2. 图层命名
- ✅ 必须严格按照命名规范表中的中文命名
- ✅ 可以用简写（例如 `左鞋` 而不是 `鞋袜-左`）
- ❌ 不能随意自创命名（脚本无法识别）
- ❌ 配饰类必须指定具体物品（不能只写 `配饰-头部`）

### 3. 图层可见性
- ✅ 只有可见的图层会被处理
- ✅ 隐藏的参考图、草稿不会被处理
- ✅ 如果某个图层不想整理，隐藏它即可

### 4. 画布和位置
- ✅ 脚本不会改变画布尺寸
- ✅ 脚本不会改变图层在画布上的位置
- ✅ 只会重新组织图层的分组结构和命名

### 5. 未识别的图层
- 如果有图层命名不符合规范，脚本会在结果对话框中列出
- 这些图层不会被处理，保持原样

---

## 🔧 如何添加新的配饰物品

如果遇到词典中没有的新物品，可以自己添加到脚本中：

1. 用记事本或代码编辑器打开 `spine_organizer.jsx`
2. 找到对应的词典部分（在脚本开头）：
   - `HANDHELD_ITEMS` — 手持物品
   - `FRONT_BODYWEAR` — 身体前配饰
   - `BACK_BODYWEAR` — 身体后配饰
   - `HEADWEAR` — 头部配饰
3. 按照格式添加新物品：

```javascript
// 例如：添加手持物品"魔杖"
var HANDHELD_ITEMS = {
  "魔杖": "magicwand",  // 注意：英文代码只能是一个单词
  "平底锅": "pan",
  // ... 其他物品
};
```

4. 保存文件后重新运行脚本

---

## 📝 完整工作流程示例

假设你要整理一个 `deer.psd` 文件：

### 步骤 1：命名图层
```
头发-前发
头发-后发
左鞋
右鞋
上装-左袖
上装-右袖
上装-身体
裙子
配饰-头部鹿角
配饰-左手拿法杖
眼睛-左眼白
眼睛-右眼白
眼睛-左眼珠
眼睛-右眼珠
嘴
```

### 步骤 2：运行脚本
1. 文件 → 脚本 → 浏览 → 选择 `spine_organizer.jsx`
2. 风格名自动显示为 `deer`
3. 点击"开始整理"

### 步骤 3：查看结果
脚本执行完成后会显示：
```
整理完成！成功 15 / 15

========== 验证命名正确性 ==========
✅ 所有检查通过！命名结构正确。
```

### 步骤 4：检查图层面板
你会看到所有图层已经按照 `[slot] → [skin] → attachment` 三层结构整理好了！

---

## 🆘 常见问题

### Q1：脚本提示"未识别图层"怎么办？
**A**：检查图层命名是否完全符合规范。可以参考本文档的命名规范表，或者先点"预览"查看哪些图层无法识别。

### Q2：风格名不对怎么办？
**A**：在弹出的对话框中可以手动修改风格名，然后再点"开始整理"。

### Q3：整理完成后还可以修改吗？
**A**：可以，但建议不要手动改动生成的结构。如果需要调整，建议先撤销（Ctrl+Z），修改原始图层命名后重新运行脚本。

### Q4：可以只整理部分图层吗？
**A**：可以，把不需要整理的图层隐藏即可，脚本只会处理可见图层。

### Q5：脚本会修改原始 PSD 吗？
**A**：会直接修改当前打开的 PSD。建议运行前先备份或者另存为一个新文件。

---

## 📞 技术支持

如果遇到问题或需要添加新的命名规则，请联系开发者。

---

## 📄 附录：快速命名检查清单

打印或保存这个清单，用于快速检查命名是否正确：

**基础部位：**
- [ ] 头发：`头发-前发`、`头发-后发`
- [ ] 鞋袜：`左鞋`、`右鞋`、`左袜`、`右袜`
- [ ] 上装：`左袖`、`右袖`、`身体`（或带上下的版本）
- [ ] 下装：`裙子` / `左裤`、`右裤`
- [ ] 五官：`左眉`、`右眉`、`嘴`
- [ ] 眼睛：`左眼白`、`右眼白`、`左眼珠`、`右眼珠`、`左睫毛`、`右睫毛`

**配饰部位：**
- [ ] 手持：`配饰-左手拿{物品}`、`配饰-右手拿{物品}`
- [ ] 身体前：`配饰-身体前{物品}`
- [ ] 身体后：`配饰-身体后{物品}`
- [ ] 头部：`配饰-头部{物品}`

**文件检查：**
- [ ] PSD 文件名是英文（作为风格名）
- [ ] 所有需要处理的图层都是可见的
- [ ] 参考图、草稿等已隐藏

---

**版本：** 1.0  
**最后更新：** 2024
